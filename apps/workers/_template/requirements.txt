# worker-sdk is the internal base package (installed from packages/worker-sdk)
# Add your country-specific dependencies below

# HTTP clients
httpx>=0.27
requests>=2.31

# Data analysis
pandas>=2.2

# worker-sdk is installed via Dockerfile — do not pin it here
